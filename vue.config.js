module.exports = {
    publicPath: './', // Cambiar las rutas absolutas a relativas
    outputDir: 'dist', // Carpeta de salida del build
    assetsDir: 'assets', // Carpeta para los recursos estáticos
};
